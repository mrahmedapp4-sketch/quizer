# دليل تشغيل ClassQuiz بدون إنترنت (خطوة بخطوة)

البرنامج ده معمول عشان يشتغل على اللاب توب بتاعك والطلاب يدخلوا عليه من الموبايلات في الفصل بدون أي إنترنت.

## 1. تثبيت برنامج Node.js (مرة واحدة فقط)
عشان تشغل البرنامج، لازم يكون عندك "المحرك" بتاعه على اللاب توب:
1. افتح المتصفح ونزل النسخة دي: [https://nodejs.org/dist/v20.11.1/node-v20.11.1-x64.msi](https://nodejs.org/dist/v20.11.1/node-v20.11.1-x64.msi)
2. سطبه زي أي برنامج عادي (Next -> Next -> Finish).
3. **مهم جداً**: بعد التسطيب، اقفل أي "شاشة سوداء" (PowerShell أو CMD) وافتحها من جديد.

## 2. تجهيز البرنامج (أول مرة)
1. افتح مجلد المشروع (المكان اللي فيه الملفات دي).
2. دوس كليك يمين في أي مكان فاضي في المجلد واختار **Open in Terminal** أو **Open PowerShell here**.
3. اكتب الأوامر دي بالترتيب واستنى كل واحد يخلص:
   ```powershell
   pnpm install
   pnpm run build
   ```

## 3. تشغيل البرنامج في كل حصة
1. افتح شاشة الأوامر في المجلد واكتب:
   ```powershell
   pnpm start
   ```
2. هتظهر رسالة `serving on port 5000 at 0.0.0.0` .. سيب الشاشة دي مفتوحة طول الحصة.

## 4. دخول الطلاب من الموبايلات
1. افتح "نقطة اتصال" (Hotspot) من موبايلك أو اللاب توب وخلّي الكل يتصل عليها.
2. اعرف رقم الـ IP بتاع اللاب توب:
   - في نفس الشاشة السوداء، اكتب `ipconfig`.
   - دور على `IPv4 Address` (بيكون حاجة زي `192.168.1.5`).
3. الطلاب يفتحوا المتصفح ويكتبوا: `http://192.168.1.5:5000` (غير الرقم باللي ظهر عندك).

---
**ملاحظة**: لو ظهرت رسالة من "جدار الحماية" (Windows Firewall)، اختار **Allow Access** عشان الموبايلات تقدر تشوف اللاب توب.
